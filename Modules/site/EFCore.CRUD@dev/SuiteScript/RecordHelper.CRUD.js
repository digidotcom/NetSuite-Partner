define('RecordHelper.CRUD', [
    'underscore',
    'RecordHelper'
], function RecordHelperDefine(
    _,
    RecordHelper
) {
    'use strict';

    _(RecordHelper.prototype).extend({

    });

    return RecordHelper;
});
